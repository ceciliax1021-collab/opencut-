# OpenCut 分发指南

---

## 一、Mac 用户 —— 现在就能发

打开文件夹：`Downloads/opencut/release/`

把这个发给对方：

**`OpenCut-分发包-Mac版.zip`**

对方解压 → 双击 `.dmg` → 拖进应用程序 → 完成。

---

## 二、Windows 用户 —— 为什么 Mac 上不能直接做？

打个比方：
- Mac 安装包 = `.dmg`（苹果格式）
- Windows 安装包 = `.exe`（微软格式）

**在 Mac 上没法直接做出 `.exe`**，就像 Word 打不开 Keynote 一样。

### 你有两个选择

#### 选择 A：GitHub 免费云端打包（推荐，不用 Windows 电脑）

打开 **`release/Windows安装包获取方式.txt`**，只有 3 步：

```
1. 注册 github.com
2. 上传 OpenCut-源码-上传GitHub.zip
3. Actions 里点 Run workflow → 下载 setup.exe
```

#### 选择 B：找一台 Windows 电脑

把整个 `opencut` 文件夹拷过去，运行：

```bash
npm install
npm run build
```

安装包在 `src-tauri\target\release\bundle\nsis\` 里。

---

## 三、更新后数据会丢吗？

**不会。**

数据不在安装包里，在系统目录：

- Mac：`~/Library/Application Support/com.opencut.desktop/clips/`
- Windows：`%APPDATA%\com.opencut.desktop\clips\`

覆盖安装新版本 → **数据还在**。详见 `release/数据与更新说明.txt`。

---

## 四、release 文件夹说明

| 文件 | 干什么 |
|------|--------|
| `OpenCut-分发包-Mac版.zip` | 发给 Mac 用户 |
| `OpenCut-源码-上传GitHub.zip` | 上传 GitHub 打 Windows 包 |
| `Windows安装包获取方式.txt` | Windows 三步教程 |
| `数据与更新说明.txt` | 更新/备份说明 |
| `安装说明.txt` | 给最终用户看的 |

---

## 五、以后更新版本

```bash
cd ~/Downloads/opencut
npm run build
./scripts/准备分发包.sh
./scripts/打包GitHub源码.sh
```

Mac 包装进 zip，Windows 重新走一遍 GitHub Actions 即可。
